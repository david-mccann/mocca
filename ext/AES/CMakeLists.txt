CMAKE_MINIMUM_REQUIRED(VERSION 3.2.0)
PROJECT(AES LANGUAGES CXX)

ADD_LIBRARY(AES
	src/AES.cpp)

SET_PROPERTY(TARGET AES PROPERTY CXX_STANDARD 11)
	
TARGET_INCLUDE_DIRECTORIES(
	AES PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
	